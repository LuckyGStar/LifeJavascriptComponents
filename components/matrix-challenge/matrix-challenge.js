const analyse = (matrix) => {
  const rowCount = matrix.length;
  const colCount = matrix[0].length;

  for (let i = 0; i < rowCount; i++) {
    for (let j = 0; j < colCount; j++) {
      const value = matrix[i][j];
    }
  }
  return matrix;
};

exports.analyse = analyse;
